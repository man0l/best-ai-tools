export interface Tool {
  title: string;
  description: string;
  imageUrl: string;
  filter1: string;
  url: string;
  page?: string;
  Tags?: string;
  rank?: number;
  // Add any additional fields that your API returns
} 